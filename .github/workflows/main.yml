# name: Build and Deploy

# on:
#   push:
#     branches:
#       - main # Change this to your default branch

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v2

#     - name: Set up Node.js
#       uses: actions/setup-node@v2
#       with:
#         node-version: 18 # You can change the Node.js version as needed

#     - name: Install dependencies
#       run: npm install

#     - name: Build Docker image
#       run: |
#         docker build -t nodeimage:${{ github.sha }} .
#         docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
#         docker push nodeimage:${{ github.sha }}


name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build . -t ${{ secrets.DOCKER_USERNAME }}/nodeimage:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/nodeimage:latest
